<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.calc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Calc">
        <activity android:name=".MainActivityNight"></activity>
        <activity android:name=".images.SharkActivity" />
        <activity android:name=".images.GoatActivity" />
        <activity android:name=".images.HorseActivity" />
        <activity android:name=".images.MouseActivity" />
        <activity android:name=".images.CatActivity" />
        <activity android:name=".ImageViewActivity" />
        <activity android:name=".ActivityCalculator" />
        <activity android:name=".CalculatorActivity" />
        <activity android:name=".calculator" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>